/** Extra pause after the success chime finishes (correct answer) before Spanish playback. */
export const POST_SUCCESS_EXTRA_PAUSE_MS = 0;

/** Auto-advance "Next phrase" on the feedback screen when TTS is idle. */
export const FEEDBACK_AUTO_ADVANCE_MS = 2000;

/** Auto-advance for the "Explain that again" pill (progress + timeout), recording and feedback. */
export const EXPLAIN_ACK_AUTO_ADVANCE_MS = 4000;
