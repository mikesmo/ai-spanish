// Platform-agnostic re-export — bundlers resolve .web.ts or .native.ts automatically.
// If your bundler doesn't support platform extensions, import the platform file directly.
export * from './index.web';
