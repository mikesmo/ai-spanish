diff --git a/node_modules/react-native-deepgram/android/src/main/java/com/deepgram/DeepgramModule.kt b/node_modules/react-native-deepgram/android/src/main/java/com/deepgram/DeepgramModule.kt
index 82b1213..c3ee5e2 100644
--- a/node_modules/react-native-deepgram/android/src/main/java/com/deepgram/DeepgramModule.kt
+++ b/node_modules/react-native-deepgram/android/src/main/java/com/deepgram/DeepgramModule.kt
@@ -40,9 +40,9 @@ class DeepgramModule(private val reactContext: ReactApplicationContext) :
   private val bufferSize: Int
     get() = minBufferSize * 4
 
-  private var audioRecord: AudioRecord? = null
-  private var recordingThread: Thread? = null
-  private var isRecording = false
+  @Volatile private var audioRecord: AudioRecord? = null
+  @Volatile private var recordingThread: Thread? = null
+  @Volatile private var isRecording = false
   private var acousticEchoCanceler: AcousticEchoCanceler? = null
   private var noiseSuppressor: NoiseSuppressor? = null
   private var automaticGainControl: AutomaticGainControl? = null
@@ -91,6 +91,15 @@ class DeepgramModule(private val reactContext: ReactApplicationContext) :
 
       enableAudioEffects(recorder)
       recorder.startRecording()
+
+      if (recorder.recordingState != AudioRecord.RECORDSTATE_RECORDING) {
+        recorder.release()
+        audioRecord = null
+        releaseAudioEffects()
+        promise.reject("start_failed", "AudioRecord did not enter RECORDING state after startRecording()")
+        return
+      }
+
       isRecording = true
 
       startRecordingThread()
@@ -107,6 +116,7 @@ class DeepgramModule(private val reactContext: ReactApplicationContext) :
   private fun stopRecordingInternal(throwOnError: Boolean = true) {
     isRecording = false
     recordingThread?.interrupt()
+    try { recordingThread?.join(500) } catch (_: InterruptedException) { }
     recordingThread = null
 
     try {
diff --git a/node_modules/react-native-deepgram/ios/Deepgram.mm b/node_modules/react-native-deepgram/ios/Deepgram.mm
index 516c89a..34270cd 100644
--- a/node_modules/react-native-deepgram/ios/Deepgram.mm
+++ b/node_modules/react-native-deepgram/ios/Deepgram.mm
@@ -206,11 +206,14 @@ - (BOOL)activateAudioSession:(NSError **)outError
 - (void)deactivateAudioSession
 {
   DGLogDebug(@"[Deepgram] deactivateAudioSession: begin");
-  NSError *error = nil;
-  if (![[AVAudioSession sharedInstance] setActive:NO error:&error] && error) {
-    DGLogError(@"[Deepgram] Failed to deactivate audio session: %@",
-          error.localizedDescription ?: error);
-  }
+  // Tearing down the mic must NOT call `[AVAudioSession setActive:NO]` on the
+  // *shared* session: doing so interrupts any in-flight playback owned by
+  // another audio module (e.g. expo-av playing a TTS clip), cutting it off
+  // mid-word. Deepgram's guard (`maybeDeactivateAudioSession`) only inspects
+  // its own record/player state, so it cannot know another module is still
+  // playing. We simply mark the session unconfigured; the next
+  // `startRecording` re-runs `configureAudioSession` + `setActive:YES`. The
+  // shared session staying active between clips is harmless.
   self.audioSessionConfigured = NO;
 }
 
