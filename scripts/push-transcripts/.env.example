# Same Supabase project as apps/web / migrate-lesson-weights / tts-batch (never commit real keys).

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co

# Service role key (Dashboard → Settings → API). Bypasses RLS — treat like a password.
SUPABASE_SERVICE_ROLE_KEY=
